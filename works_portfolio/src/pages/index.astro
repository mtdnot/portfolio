---
const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;

const profile = {
  name: '松田直和',
  handle: '@mtdnot',
  birthday: '2006/11/29',
  birthplace: '三重県津市久居',
  affiliations: [
    { label: '会津大学', href: '/u-aizu/' },
    { label: 'Frexida', href: '#' },
  ],
  achievements: [
    {
      prefix: '2026年度',
      label: 'アオタケプロジェクト',
      href: 'https://aizu-startups-foundation.com/aotake/top',
      suffix: 'に採択',
    },
  ],
  sns: [
    { label: 'GitHub', href: 'https://github.com/mtdnot/' },
    { label: 'SpeakerDeck', href: 'https://speakerdeck.com/mtdnot' },
    { label: 'Facebook', href: 'https://www.facebook.com/profile.php?id=61559472083047' },
    { label: 'LinkedIn', href: 'https://www.linkedin.com/in/%E7%9B%B4%E5%92%8C-%E6%9D%BE%E7%94%B0-917706390/' },
  ],
  blog: {
    label: '索(あなぐる)',
    href: 'https://blog.mtdnot.dev',
  },
  footerLinks: [
    {
      label: '個人',
      caption: 'personal portfolio',
      href: '/',
    },
    {
      label: '仕事',
      caption: 'works portfolio',
      href: '/works/',
    },
    {
      label: '技術',
      caption: 'tech portfolio',
      href: '/tech_portfolio/',
    },
    {
      label: '大学',
      caption: 'u-aizu portfolio',
      href: '/u-aizu/',
    },
  ],
  contact: 'mtdnot@frexida.com',
};
---

<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{profile.name} | works portfolio</title>
    <link rel="icon" type="image/png" href={`${base}images/icon.png`} />
    <link rel="icon" type="image/png" sizes="32x32" href={`${base}images/icon.png`} />
    <link rel="icon" type="image/png" sizes="16x16" href={`${base}images/icon.png`} />
    <meta
      name="description"
      content={`${profile.name}の仕事用ポートフォリオ。所属、実績、SNS、連絡先をまとめたページ。`}
    />
  </head>
  <body>
    <main class="page">
      <section class="profile-block">
        <h1>Profile</h1>

        <div class="profile-visuals">
          <img src={`${base}images/photo.png`} alt={`${profile.name}の写真`} class="portrait-image" />
          <img src={`${base}images/icon.png`} alt={`${profile.name}のアイコン`} class="portrait-image" />
        </div>

        <p class="name-line">
          {profile.name}
          <span class="handle">({profile.handle})</span>
        </p>

        <p class="mail">[mail] {profile.contact}</p>

        <div class="content-block">
          <div class="section">
            <h2># Basic</h2>
            <ul>
              <li>誕生日 : {profile.birthday}</li>
              <li>出身 : {profile.birthplace}</li>
            </ul>
          </div>

          <div class="section">
            <h2># 所属</h2>
            <ul>
              {profile.affiliations.map((item) => (
                <li>
                  {item.href === '#' ? item.label : <a href={item.href}>{item.label}</a>}
                </li>
              ))}
            </ul>
          </div>

          <div class="section">
            <h2># 実績</h2>
            <ul>
              {profile.achievements.map((item) => (
                <li>
                  {item.prefix}<a href={item.href} target="_blank" rel="noreferrer">{item.label}</a>{item.suffix}
                </li>
              ))}
            </ul>
          </div>

          <div class="section">
            <h2># Social</h2>
            <ul>
              {profile.sns.map((item) => (
                <li><a href={item.href} target="_blank" rel="noreferrer">{item.label}</a></li>
              ))}
            </ul>
          </div>

          <div class="section">
            <h2># Blog</h2>
            <ul>
              <li><a href={profile.blog.href} target="_blank" rel="noreferrer">{profile.blog.label}</a></li>
            </ul>
          </div>
        </div>

        <footer class="portfolio-footer">
          {profile.footerLinks.map((item) => (
            <a class="footer-card" href={item.href}>
              <span class="footer-label">{item.label}</span>
              <span class="footer-caption">{item.caption}</span>
            </a>
          ))}
        </footer>
      </section>
    </main>
  </body>
</html>

<style>
  @font-face {
    font-family: "SmartFontUI";
    src: url("/fonts/smartfont-ui.otf") format("opentype");
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }

  :root {
    --bg: #060b14;
    --panel: #0d1627;
    --panel-strong: #101d34;
    --text: #edf4ff;
    --muted: #8fa4c5;
    --line: #1f3558;
    --accent: #5fa8ff;
  }

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    font-family: "SmartFontUI", "Hiragino Sans", "Noto Sans JP", sans-serif;
    background:
      radial-gradient(circle at top, rgba(78, 133, 224, 0.16) 0%, rgba(78, 133, 224, 0) 32%),
      linear-gradient(180deg, #08101c 0%, var(--bg) 100%);
    color: var(--text);
  }

  a {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 0.15em;
  }

  a:hover {
    opacity: 0.75;
  }

  .page {
    max-width: 980px;
    margin: 0 auto;
    padding: 48px 24px 80px;
  }

  .profile-block {
    max-width: 860px;
    margin: 0 auto;
  }

  h1 {
    font-size: 2.1rem;
    margin-bottom: 1.5rem;
    font-weight: 600;
    text-align: center;
  }

  .profile-visuals {
    display: flex;
    gap: 16px;
    justify-content: center;
    align-items: flex-start;
    margin-bottom: 1rem;
  }

  .portrait-image {
    width: 156px;
    height: 156px;
    aspect-ratio: 1;
    object-fit: cover;
    display: block;
    border: 1px solid var(--line);
    box-shadow: 0 14px 30px rgba(0, 0, 0, 0.32);
  }

  .name-line {
    font-size: 1.2rem;
    margin-bottom: 0.4rem;
    text-align: center;
  }

  .handle {
    color: var(--muted);
  }

  .mail {
    margin-bottom: 0.7rem;
    text-align: center;
  }

  .content-block {
    background: rgba(13, 22, 39, 0.92);
    border: 1px solid var(--line);
    border-radius: 18px;
    padding: 24px 28px;
    box-shadow: 0 24px 48px rgba(0, 0, 0, 0.28);
  }

  .portfolio-footer {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 12px;
    margin-top: 18px;
  }

  .footer-card {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    padding: 16px 18px;
    border: 1px solid var(--line);
    border-radius: 16px;
    background: rgba(16, 29, 52, 0.92);
    color: var(--text);
    text-decoration: none;
    transition: background 120ms ease, transform 120ms ease, border-color 120ms ease;
  }

  .footer-card:hover {
    opacity: 1;
    background: #152542;
    border-color: #2f4f83;
    transform: translateY(-1px);
  }

  .footer-label {
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0.02em;
  }

  .footer-caption {
    font-size: 0.86rem;
    color: var(--muted);
  }

  .section + .section {
    margin-top: 1.4rem;
  }

  .section h2 {
    font-size: 1rem;
    margin-bottom: 0.45rem;
    font-weight: 600;
  }

  ul {
    list-style: disc;
    padding-left: 1.25rem;
  }

  li {
    line-height: 1.8;
  }

  @media (max-width: 900px) {
    .profile-visuals {
      align-items: center;
    }
  }

  @media (max-width: 640px) {
    .page {
      padding: 32px 16px 56px;
    }

    .portfolio-footer {
      grid-template-columns: 1fr;
    }

    .profile-visuals {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
